{
  "rewrites": [
    { "source": "/(.*)", "destination": "/app/page.tsx" }
  ],
  "builds": [
    {
      "src": "app/**/*",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/app/page.tsx"
    }
  ]
}
